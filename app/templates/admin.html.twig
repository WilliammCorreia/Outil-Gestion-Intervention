<html>
    <head>
        <meta charset="utf-8"/>
        <title>AccordEnergie</title>
        <link rel="stylesheet" href="style/register.css"/>
    </head>
</html>
{% block content %}
    <body>
        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Email</th>
                    <th>Nom</th>
                    <th>Prénom</th>
                    <th>Code Postal</th>
                    <th>Statut</th>
                </tr>
            </thead>
            <tbody>
            {% for user in users %}
                <tr>
                    <td>{{ user.id }}</td>
                    <td>{{ user.email }}</td>
                    <td>{{ user.last_name }}</td>
                    <td>{{ user.first_name }}</td>
                    <td>{{ user.postal_nb }}</td>
                    <td>{{ user.statut }}</td>
                    {# <td><button href="{{ $page->deleteUser(user.id) }}">Modifier</button></td> #}
                    <td><a href="{{ Page::deleteUser(user.id) }}">Supprimer</a></td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </body>
{% endblock %}
